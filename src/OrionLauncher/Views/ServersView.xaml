<UserControl x:Class="OrionLauncher.Views.ServersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DataGrid Grid.Row="0"
                  ItemsSource="{Binding Servers}"
                  SelectedItem="{Binding SelectedServer}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  SelectionMode="Single"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column"
                  RowBackground="#1E1E1E"
                  AlternatingRowBackground="#252525"
                  BorderBrush="#444"
                  Foreground="#DDD">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Name"        Binding="{Binding Name}"        Width="*"   IsReadOnly="True"/>
                <DataGridTextColumn Header="Address"     Binding="{Binding Address}"      Width="160" IsReadOnly="True"/>
                <DataGridTextColumn Header="Region"      Binding="{Binding Region}"       Width="80"  IsReadOnly="True"/>
                <DataGridTextColumn Header="Max Players" Binding="{Binding MaxPlayers}"   Width="90"  IsReadOnly="True"/>
                <DataGridTextColumn Header="Description" Binding="{Binding Description}"  Width="200" IsReadOnly="True"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Required mods chips -->
        <Border Grid.Row="1" Background="#1A1A1A" Padding="8,4" Margin="0,4,0,0"
                Visibility="{Binding SelectedServer, Converter={StaticResource NullToVisibility}}">
            <WrapPanel>
                <TextBlock Text="Required mods: " Foreground="#888" VerticalAlignment="Center" Margin="0,0,6,0"/>
                <ItemsControl ItemsSource="{Binding SelectedRequiredMods}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate><WrapPanel/></ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="#2A4A6A" CornerRadius="10" Padding="8,2" Margin="2">
                                <TextBlock Text="{Binding}" Foreground="#8CF" FontSize="11"/>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </WrapPanel>
        </Border>

        <Button Grid.Row="2"
                Content="Connect to Selected Server"
                Command="{Binding ConnectCommand}"
                HorizontalAlignment="Left"
                Margin="4,8,4,4"
                Padding="14,6"
                Style="{StaticResource PrimaryButtonStyle}"/>

        <TextBlock Grid.Row="3" Text="{Binding StatusText}" Foreground="#888"
                   Margin="4,4,4,0" FontSize="11"/>
    </Grid>
</UserControl>
